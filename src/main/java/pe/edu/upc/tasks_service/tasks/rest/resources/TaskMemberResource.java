package pe.edu.upc.tasks_service.tasks.rest.resources;

public record TaskMemberResource(Long id,
                                 String name,
                                 String surname,
                                 String urlImage) {
}
